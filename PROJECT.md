# [Goose] — In-House Legal Agent Platform

Custom distribution of Block's Goose (now AAIF / Linux Foundation) — `aaif-goose/goose`. We replace the UI layer and the memory layer; we leave the Rust agent core alone.

## The one goal (short-term)

1. Fork Goose (done — `sarturko-maker/oscar-gc`, mirroring `aaif-goose/goose`; renamed from `sarturko-maker/goose` at rebrand-2026-05-23, GitHub auto-redirects old URLs).
2. Replace the UI layer (`ui/desktop/src/`) with an in-house-legal UI: practice areas → primary unit → memory + artifacts + agent.
3. Replace the memory layer with a scoped MCP server we own.
4. Wire adeu as an MCP server for the Commercial practice area.

Nothing further is in scope until those four are working.

## Fork strategy

We are a custom distribution of Block's Goose (now AAIF / Linux Foundation) per upstream's [`CUSTOM_DISTROS.md`](./CUSTOM_DISTROS.md) — not a hard fork. `CLAUDE.md` "Fork hygiene" covers per-PR discipline (Rust core untouched, product work in `ui/desktop/src/` + sibling crates). This section covers the upstream relationship.

**Canonical upstream**: `aaif-goose/goose` (formerly `block/goose` — the project moved to the AI Alliance Foundation under the Linux Foundation in late 2025). Remotes on `lq-vps`:

| Remote | URL | Purpose |
|---|---|---|
| `origin` | `git@github.com:sarturko-maker/oscar-gc.git` | Our distribution; product code lives here. (Renamed from `sarturko-maker/goose` at rebrand-2026-05-23; old URL auto-redirects.) |
| `upstream` | `git@github.com:aaif-goose/goose.git` | Canonical upstream; tracked for releases and breaking changes. |

**Customization route**: upstream's `CUSTOM_DISTROS.md` documents the supported surfaces for a custom distribution — desktop branding metadata (`ui/desktop/package.json`, `forge.config.ts`, `forge.{deb,rpm}.desktop`, `index.html`), UI rewrite under `ui/desktop/src/`, sibling crates for our additions (memory MCP server, practice-area config, adeu integration), and the system prompt at `crates/goose/src/prompts/system.md`. We follow that path; we do not patch the Rust core.

**Upstream-tracking cadence**: weekly read of upstream release notes. For each release we decide one of:

- **Skip** — release contains no security-, fork-, or product-relevant changes for us.
- **Merge** — pull `upstream/main` into a feature branch, resolve conflicts, merge into our `main`.
- **Wait** — defer to the next weekly cycle (e.g., release looks risky; want to see hotfixes land first).

Capture the per-release decision as a one-line note in `SPRINT_LOG.md` under the active sprint, or as an ADR if the decision is structural (e.g., declining a breaking change because we have a competing customization).

## Distribution shape

Oscar GC ships as a single installable binary — `.dmg`, `.exe`, `.AppImage`. The user downloads one file, double-clicks, it runs. No "also run this service," no "install this dependency."

The sibling MCP server repos (`oscar-memory-mcp`, `oscar-onboarding-mcp`, future MCP servers) exist as separate repos for development convenience — fork hygiene against upstream Goose, per-stack build cleanliness, independent lifecycle. **They are not separate at distribution.** Each MCP server's built output gets bundled into the Electron package at packaging time, copied into the resources directory alongside `goosed`. At runtime, the desktop agent spawns them as embedded subprocesses, transparent to the user.

Pattern mirrors how Goose already handles `goosed` — separation in development, bundling at build, single binary at distribution.

Three rules to preserve this option as MCP servers grow:

1. **No system-level dependencies.** No "this requires Postgres" or "this needs a Python interpreter." Embed SQLite if you need persistence; use Node built-ins for HTTP. Anything that survives bundling (esbuild single-file output, `pkg`/Bun-compiled native binaries) is fine; anything that needs OS-level install isn't.

2. **Small, self-contained.** Each MCP server is a few hundred lines, minimal dependencies, no heavy native modules. Bundle-shaped from the start.

3. **No cross-MCP runtime coupling.** Servers don't assume other servers' versions, schemas, or presence at runtime. Each is independent. Cross-MCP coordination happens through the agent that calls them, not through direct server-to-server channels.

Bundling itself becomes a sprint when there are enough MCP servers and a clear "we're ready to ship" trigger — likely Sprint 12-15 range, after the four-item short-term goal completes. Until then, dev separation is right; the rules above keep the door open.

## Sprint Index

| Sprint | Goal | Status |
|---|---|---|
| 1 | Unmodified Goose builds + MiniMax round-trip on `lq-vps`. No product code. | Closed 2026-05-17 |
| 2 | Oscar GC rebrand (branding metadata only). First custom-distribution cycle. | Closed 2026-05-18 |
| 3 | First `ui/desktop/src/` source change — Oscar GC landing placeholder. LQdesign Terminal default. | Closed 2026-05-18 |
| 4 | Practice-area sidebar (13 entries) + placeholder routes. Seam moves to AppLayout. | Closed 2026-05-18 |
| 4.5 | Visual fidelity audit + gap closure against LQdesign Terminal: JetBrains Mono + Cormorant Garamond loaded; eyebrow / orb / `01`–`13` mono numerics / indigo-tinted sidebar active state applied. | Closed 2026-05-18 |
| 4.6 | Editorial as Oscar GC's default surface; supersedes ADR-004. Cream paper, Cormorant Garamond hero with italic copper accent, IBM Plex Mono eyebrows with hairline rule, Outfit body, copper-glow active sidebar. Outfit + IBM Plex Mono loaded; class scope renamed `.oscar-terminal` → `.oscar`. | Closed 2026-05-18 |
| 5 | Minimal in-house memory MCP server. New sibling repo `sarturko-maker/oscar-memory-mcp`. Two-tool stdio extension registered with Goose CLI; verified end-to-end. | Closed 2026-05-18 |
| 6 | First-launch onboarding as an agent-driven interview. New sibling repo `sarturko-maker/oscar-onboarding-mcp` with `finalize_profile` tool; recipe-scoped onboarding agent in a dedicated Editorial chat surface; `~/.config/oscar/profile.json` schema (v1); sidebar runtime-reads the profile. | Closed 2026-05-18 |
| 7 | Onboarding dogfood — CC operates Oscar GC as a real in-house lawyer (two personas, primary + edge) against real MiniMax. Surfaces one P0 (closing-message race), four P1s, six P2s. Establishes the "CC as the user" dogfood pattern; harness committed under `scripts/dogfood/`. Report at `docs/dogfood/sprint-7/`. | Closed 2026-05-18 |
| 8 | Hub welcome banner closes Sprint 7's P0 closing-message race. ADR-015 (extends ADR-014). `OscarHubBanner` reads `profile.json` directly; dismissable; localStorage persisted. Closes P0-A, P1-D; deprecates P1-A / P1-C as load-bearing failures. P1-B deferred to Sprint 9. Re-dogfood confirms all four. Dogfood harness gains env-configurable screenshot base + `click <selector>` subcommand. Report at `docs/dogfood/sprint-8/`. | Closed 2026-05-18 |
| 9 | adeu==1.6.9 wired as a stdio MCP under capability name `redline`. Python venv at `/srv/projects/oscar-runtime/python/adeu-venv/` (first Python component). Five ADRs at decision time: 016 Python runtime, 017 capability binding (config-level DI), 018 path-as-text ingress, 019 disk-write egress, 020 five-step lawyer-reasoning system prompt. Commercial recipe scaffolds the chat under `ui/desktop/src/components/oscar/commercial/`. Round-trip verified: substantive unilateral NDA made mutual via 8 coordinated `process_document_batch` edits; OOXML walk + lawyer-quality comparison committed. **Closes the fourth (and final) item of the four-item short-term goal.** Report at `docs/dogfood/sprint-9/`. | Closed 2026-05-18 |
| 10 | Oscar GC as one-step Crostini install. Single `oscar-gc_1.34.0_amd64.deb` published to GitHub Release. Bundled: relocatable CPython 3.12 + adeu wheels (postinst venv), Node 24, esbuild MCP bundles, launcher wrapper. Debian 12 Docker build host (ABI compatibility). Renderer blocker root-caused in 5 dogfood iterations (preload sandbox cannot import `node:fs` — fixed by moving probe to main.ts + additionalArguments config channel). Ten ADRs (021–030). CLAUDE.md inverted-defaults doctrine applied: telemetry suppressed, recipe-trust dialog bypassed for bundled recipes, GooseLogo replaced with LQ mark, Settings affordance restored. | Closed 2026-05-18 |
| 11 | claude-for-legal repackaged as bundled in-house skill library. Anthropic's 9 in-house plugins vendored at upstream commit `4d55f539` (Apache 2.0); 111 SKILL.md files. 27 drops (per-plugin `cold-start-interview`, `customize`, `CLAUDE.md` × 9) + 9 `matter-workspace` stubs kept (Sprint 12 supersedes) + 6 collision renames (`<plugin>__<skill>` per ADR-031 detect-and-prefix). Onboarding schema v2 with `area_profile` per area; new `list_area_questions` MCP tool sourced from `OSCAR_RESOURCES_ROOT`; P3.5 per-area mini-interview phase with 2-question hard cap. Five ADRs (031–035): practice-area→plugin mapping, onboarding schema v2, gating-strip policy, output-path convention (supersedes ADR-019), Apache 2.0 NOTICE + attribution. Two Sprint 10 carry-forwards closed: onboarding markdown rendering, P3→P3.5 pacing. Dogfood deferred to Arturs's Chromebook. | Closed 2026-05-19 |
| 12 | Matters, Forge, and the access model formalisation. Matters as first-class scoped containers per practice area (`~/.config/oscar/state/<area-id>/matters/<slug>/`); one-matter-↔-one-session binding via `matters.json[slug].session_id`. Matter scope-down via per-session recipe with `oscar-fs` allowed-directories narrowed to the matter folder. Top of Mind matter context via Goose's `tom` platform extension (`GOOSE_MOIM_MESSAGE_FILE` → stable `~/.config/oscar/tom-active-matter.md`; matters IPC writes on set-active / truncates on detach). Forge meta-agent in a new sidebar header zone (above practice areas); two-mode contour (create-skill / create-area); deliberately narrow scope (`~/.agents/skills/` + `~/.config/oscar/`). Filesystem MCP via vendored `@modelcontextprotocol/server-filesystem@2026.1.14` at capability name `oscar-fs`. Generic builder `buildPracticeAreaRecipe` so all 13 areas host matters; Commercial keeps adeu redline by composing on top. Network egress discipline: three-layer (per-MCP audited convention via build-time grep into `BUNDLE.json`; goosed → MiniMax only; Electron renderer CSP tightened + `webRequest` blocks non-GET to non-localhost). `GOOSE_ALLOWLIST` activated against an empty bundled allowlist (no MCP installs via UI). 48 substantive bundled skills' matter paths rewritten to `$OSCAR_MATTER_DIR`; 9 matter-workspace stubs retired. CLAUDE.md adds upstream-doc reference rule (Phase 0, before any code) — paid off in plan-mode by surfacing Top of Mind / Sandbox / Allowlist / MCP Roots. Nine ADRs (036–044), all at decision time. `TODO.md` introduced for the carry-forward backlog. | Closed 2026-05-19 |
| 14 | In-house framing for matters across 13 practice areas (Sprint 13 dogfood P2-B/C/D/E + P0-A class lesson). Matter schema v2 (ADR-047): drops law-firm vocabulary (`client`/`counterparty`/`matter_type`); adds typed `subject {type, label}` (15 subject types), `counterparty {role, name}\|null` (21 typed roles), area-typed `kind`, sparse `extras` map, `stakeholder` grouping tag, explicit `area_id` and `working_dir`. Per-area config (`practiceAreaShapes.ts`) drives one config-aware `NewMatterDialog` — no 13-way switch. Five family templates emerge (Contract / Person / Regulator-Obligation / Internal-asset / Event-shaped). Split disk layout: state in `~/.config/oscar/state/<area-id>/`, content in `~/Documents/Oscar GC/<Area>/<Matter>/` (Finder-discoverable, drag-drop, cloud-sync-compatible); `oscar-fs` allowed-directories widens for an active matter. Tag-and-group stakeholder grouping: `MattersLanding` groups MatterRows by stakeholder header with autocomplete from prior values; "Other" bucket for null-stakeholder. Matter back-affordance: new `MatterBackButton` wraps existing `BackButton` (no new pattern), IPC `oscar:matters:lookup-session` for reverse session→matter binding lookup; mounted top-left in BaseChat, "All matters" label, clears Top of Mind on click. Top of Mind renderer rewritten with adaptive template + `LABELS` dict (in-house vocab, no per-area branching). Forge sidebar alignment fix (P2-A): 56px Menu-trigger clearance moved from list to header. Bundled-MCP spawn-boot smoke test (ADR-049): `smokeTestBundledMcps` in `prepare-oscar-bundle.js` spawns each bundled MCP under bundled Node, watches stderr for ready-line regex, 3s timeout, throws on failure — Sprint 13's duplicate-shebang regression would now fail the .deb build before electron-forge make. Workstream 5 (adeu MCP App diff preview) deferred to Sprint 15 — plan-mode design captured in plan file; Phase 7 dogfood validates Units 1-2 first. Two ADRs (047, 049); 048 reserved for Sprint 15 preview UI. | Closed 2026-05-19 |
| 18 | Permissive default loadout for in-house lawyers. Doctrine flip: default ON for Memory, Top of Mind, Chat Recall, Apps, Todo, Summon, Extension Manager, Auto Visualiser; default OFF for Developer (shell + fs-write — ADR-041 access-model exclusion), Computer Controller, Tutorial, Code Mode. Two-line Rust-core touch in `crates/goose/src/agents/platform_extensions/mod.rs` flips `chatrecall.default_enabled: false → true` and `developer.default_enabled: true → false`. UI-side `bundled-extensions.json` flips `memory` + `autovisualiser` enabled-flags to true and adds a new Tavily `streamable_http` entry with the provider named in description copy (`mcp.tavily.com`). New `Tavily` entry in `INTEGRATIONS_OVERLAY` (bundled tier, Always-on badge — same posture as `oscar-fs`). New `recipe/enabledPlatformExtensions.ts` helper (filters `ConfigContext.extensionsList` to enabled platform/builtin types); threaded through `buildPracticeAreaRecipe`, `buildCommercialRecipe`, `buildForgeRecipe`. Forge force-includes `code_execution` + `Extension Manager` via `ensureForgePlatforms()` regardless of user toggles. Two call sites (`MattersLanding.openMatter`, `ForgeView`) derive from their `useConfig()` snapshots and pass through. ADR-065 picks the TS-side merge over a Rust-core `resolve_extensions_for_new_session` change. Three ADRs (063 doctrine, 064 Tavily visibility, 065 recipe builder thread-through). Crostini dogfood E1–E5 carries to Sprint 18b. | Closed 2026-05-20 on code; runtime gates → 18b |
| 19 | Chat history + memory: PA → Matter/Programme → Session, plus unscoped Quick chats. Two parallel chat-entry paths plus minimum sidebar hierarchy. Sidebar replaces its flat practice-area list with a PA → Matter → Session tree (active area auto-expands; matter row click reuses `matters.setActive` to repopulate ToM then resumes the bound session) plus a Quick chats sibling group. Unscoped quick-chat path at `~/Documents/Oscar GC/.quick-chats/` (dot-hidden, same volume as matters; Memory MCP's lazy `.goose/memory/` creation + Top of Mind's empty-file no-injection make the scratch dir safe). New `QuickChatButton` (sidebar header + Hub home variants) — onclick: `matters.detachActive()` → `quickChats.ensureDir()` → `createSession(quickChatsDir)` with no recipe (server falls back to `config.yaml` enabled extensions per `extensions.rs:125-143`, so the lawyer's full Sprint 18 permissive-default loadout is on). `PracticeAreaShape` gains required `entryNoun: { singular, plural }` — Privacy / Regulatory / AI Governance read as "Programmes"; other 10 (incl. all four Disputes per Arturs's framing) stay "Matters"; Privacy + Regulatory `kind.label` updated to "Programme type". Forge's create-area system prompt captures the noun for user-added areas (`entry_noun` field on the `practice_areas` entry in profile.json; captured-but-inert until a follow-up sprint wires shape-from-profile). New IPCs `oscar:quick-chats:ensure-dir` / `oscar:quick-chats:get-dir` exposed via `window.electron.quickChats`. `useChatHistory` hook joins `listSessions()` + `matters.list(areaId) × N` + `quickChats.getDir()`; subscribes to SESSION_CREATED/DELETED/RENAMED/FORKED for live refresh. **No Rust core touch, no schema additions.** Lean entirely on native Goose primitives. One ADR (066). Crostini dogfood E1–E8 carries to Sprint 19b. | Closed 2026-05-20 on code; runtime gates → 19b |
| 19b | Dogfood patches against Sprint 19's first build (Arturs's Crostini findings). Three surgical fixes. (1) **Matter chat-history resume** (`MattersLanding.openMatter`): pre-existing latent bug since Sprint 12 — `openMatter` always called `createSession` and rebound `matters.json[slug].session_id` to a fresh id, orphaning prior conversation. ADR-038 intended resume-on-existing; Sprint 19's sidebar exposed the contract gap by surfacing `session_id` directly. Fix: if `matter.session_id` is set AND `getSession({path:{session_id}})` returns a row, dispatch `ADD_ACTIVE_SESSION` + navigate to `/pair?resumeSessionId=` immediately (skip recipe build — the existing session has its recipe baked in). Falls through to fresh-spawn path when binding is null or session deleted out-of-band. (2) **`PopularChatTopics` removed** (`BaseChat.tsx`) — upstream Goose's dev-audience defaults (organize photos / units of geese / tamagotchi) leaked into Oscar's empty-chat surface (any session without a recipe — Sprint 19's quick-chat path). Per CLAUDE.md inverted-defaults doctrine, conditional render block + dead `showPopularTopics` prop/const removed; component file stays for i18n-extract compatibility (cleanup carry). (3) **Sidebar tree visual hierarchy + trim** (`ChatHistoryTree.tsx`, `main.css`) — matter rows now 11px italic + faded + leading `·` bullet; clearly subordinate to 13px area rows. Cap 10 most-recent matters per area in the sidebar; overflow renders "+ N more — view all" link to the area landing (full list scrolls with stakeholder grouping there). Build sha `321fd177f26450746c3b16f8510f06db3d906118a68917490a5b4bbc460f21c2` (260,154,416 bytes). Release `oscar-gc-sprint19` promoted to Latest. No new ADRs. | Closed 2026-05-20 on code; Crostini E1–E8 continues |
| 20 | M0 of the right-panel master brief — durable per-area override surface. Reconciles the brief's "agent = recipe; modify agent = modify recipe" framing with Oscar GC's runtime-built recipes: a new `area_overrides` block on `profile.json.practice_areas[i]` becomes the persistence surface for Forge-driven agent edits (description / panel sections / skills / MCPs / playbooks — all optional). Recipe builders read overrides as the final merge layer (registry default → user-added entry → override); no on-disk recipe YAMLs, no Rust touch. M0 wires `description_override` end-to-end (prepends a new `## About this practice area` block to instructions for both generic and Commercial bespoke prompts; replaces recipe-level description). Other override fields thread through types but consumed by later sub-sprints (M2 panel_sections, M4 playbooks, M5 enabled_skills, M7 enabled_mcps). Sibling `oscar-onboarding-mcp` bumped to schema v4 (`158e34d7`): `AreaOverridesSchema`, `PracticeAreaSchemaV4`, `ProfileSchemaV4`, `migrateV3ToV4`; `smoke.mjs` round-trips area_overrides and asserts v3→v4 migration. Behaviour-neutral at default — absent overrides → empty merge layer → identical recipe to Sprint 19's shape. Two ADRs (067 persistence surface, 068 schema migration). Carries M1–M8 sub-sprints. | Closed 2026-05-20 on code; Crostini E1 (description_override dogfood) → M1 |
| 29 | Right-pane Crostini polish (five issues + multi-chat research). **M1 ([[ADR-094]])**: Skills toggle bug fixed — Sprint 28's `'allow' → 'deny'` coercion treated `current.slugs` as disabled regardless of mode, flipping one Off skill on flipped every other Off skill on too; fix preserves the existing mode shape on write. Visual harness now covers the missing `mode: 'allow'` starting state. **M2 ([[ADR-095]])**: dropped `Redlining` / `Forum` / `Deadlines` coming-soon panel stubs — `redline` already lives in Sprint 28's Tools section, the others advertised functionality that does not yet ship; `PanelSectionStub.tsx` deleted (no consumers). **M3 ([[ADR-096]])**: Skills row presentation matches Tools — new `humanizeSlug` helper with 18-entry legal-acronym map (`nda-review` → "NDA Review", `saas-msa-review` → "SaaS MSA Review"); `.oscar__skills-name` flips from `mono-editorial` to `sans-editorial 12/500`; slug stays addressable on `data-testid` + `data-source` + row `title`. **M4 ([[ADR-097]])**: Skills section splits into "In this matter" surface zone (enabled only) + "All skills" collapsed `<details>` directory (everything, with drop affordance + delete); auto-opens directory when surface is empty. **M5 ([[ADR-098]])**: Edit no longer Forge-deep-links — opens an in-pane MatterFactsEditor (reuses `PracticeAreaShape`) with Save/Cancel + a labelled "Ask Forge to change this area's loadout" footer button that preserves the Forge route for power changes; new `oscar:matters:update` IPC (Zod-validated `UpdateMatterInputSchema`) writes registry + re-renders matter.md via existing `renderMatterMd`; slug immutable. `RightPaneContext` extended with `editingFacts` state; `usePanelReader` gains `refresh`. **M6 ([[ADR-099]])**: closed the Layer 2 playbook visibility gap — new `renderOnDemandPlaybooksBlock` + `oscar:playbooks:render-on-demand-block` IPC enumerates non-always-on playbooks in the recipe instructions with relative path + scope + size + first-line peek (for text formats). Inspection memo at `docs/sprint-29/playbook-wiring-inspection.md` documents the three layers + the gap + what carries (Layer 3 / 10-playbook selection remains the right deferred work). **M7 ([[ADR-100]])**: multi-chat per matter research-only — verified directly from Goose source (`goose-docs.ai` URLs probed returned 404; code prevails per CLAUDE.md): Goose auto-compacts each session at 80% of context-limit (`context_mgmt/mod.rs:185` + `agents/agent.rs:1496`), sessions are independent, `chatrecall` extension (default-on per Sprint 18) gives agent-mediated cross-session search. Sprint 30 implementation will mirror Sprint 27's per-partner pattern verbatim — `MatterEntry.session_id → sessions: SessionRef[]` with lazy v2→v3 migration; no summary-on-spawn wiring needed. Full memo at `docs/sprint-29/multi-chat-research-memo.md` with affected-surface table. **M8**: `bash scripts/build-oscar-deb.sh` → `oscar-gc_1.34.0_amd64.deb` (249 MB); two-line Sprint 28 carry-forward fix at `main.ts:3073` + `preload.ts:388` disambiguating duplicate ADR-091 / ADR-092 numbering. Per-milestone CC visual verification on lq-vps (Xvfb/Playwright) — screenshots at `docs/screenshots/sprint-29-m{1,2,3,4,5}/`, IPC probe artefacts at `docs/screenshots/sprint-29-m6/`. `tsc --noEmit` clean throughout. Seven ADRs (094 toggle fix, 095 panel stubs, 096 skills naming, 097 skills directory, 098 Edit split, 099 on-demand playbooks, 100 multi-chat research). No Rust core touch. | Closed 2026-05-25 on code; Crostini dogfood → 29b if needed |
| 28 | Right-panel polish: Edit-bug fix + Tools section + Skills toggle clarity. Three dogfood issues: clicking Edit "makes the panel disappear forever, restart does not help"; Tools and Skills compete for the same section; All/Allow/Deny mode pill is ambiguous. **M1 ([[ADR-091]])**: `useRightPaneVisibility` returns HIDDEN for non-matter `/pair` (Forge, quick-chats) — the 32-px rail-only state lawyers read as "disappeared" is removed; `NavigationContext` drops `electron-settings` persistence of `isRightPaneExpanded` so stale sticky-false can never strand the pane. **M2 ([[ADR-092]])**: new `Tools` panel section above Skills in every area; lists universal bundled (`oscar-fs` / `computercontroller` / `Tavily`) + Commercial-only `redline` as read-only "Always on", plus per-area installed integrations as toggleable On/Off. New IPCs `oscar:tools:list` + `oscar:tools:toggle` write `area_overrides.enabled_mcps = { mode: 'deny', ids }` (reuses M7's filter shape at `MattersLanding.tsx:174-180`). **M3 ([[ADR-093]])**: dropped the M5 tri-mode pill; collapsed `oscar:skills:set-mode` + `oscar:skills:toggle-slug` into single `oscar:skills:toggle(areaId, slug, enabled)`; chip text now plain "On" / "Off"; writes normalise to deny-shape. **M4**: `.oscar__skills-name` `word-break: break-all` → `overflow-wrap: anywhere` (no more mid-word breaks on kebab-slugs); `toasts.tsx` global position `top-right` → `top-center` (extension-loading toast no longer overlaps LOADOUT/Edit). **M5**: `pnpm bundle:oscar-linux` → `oscar-gc_1.34.0_amd64.deb` (260 MB). Per-milestone CC visual verification on lq-vps (Xvfb/Playwright) — screenshots at `docs/screenshots/sprint-28-m{1,2,3,4}/`. `tsc --noEmit` clean throughout. Three ADRs (091 visibility recovery, 092 Tools section, 093 Skills toggle). No Rust core touch. Carry-forward: `pnpm package` (lighter build path) requires manual `sub-recipes` copy until `bundle:oscar-linux`'s prepare step is split out. | Closed 2026-05-24 on code; Crostini dogfood → 28b if needed |
| 27 | Per-partner conversation history on the Oscar LLP roster — closes the Sprint 21 Crostini dogfood gap. `~/.config/oscar/state/oscar-llp/partners.json` schema v1 (`{session_id}`) → v2 (`{sessions: [{id, label?}, ...]}`) with read-time lazy migration per [[ADR-092]] (synthesizes `[{id: <prior session_id>, label: '(legacy)'}]` for v1 entries; mirrors [[ADR-078]]'s Lavern → Oscar LLP rename pattern). `OscarLLPRoster.tsx` (180 → 231 lines) grows an inline session list per partner card (cap 5 visible + "…N more" overflow) with three click handlers: card header → resume `sessions[0]` (preserves Sprint 21 muscle memory; falls through to fresh spawn if empty/deleted), "+ New chat with <Name>" → fresh `createSession` + prepend to `sessions[]` via `bindSession`, session row → resume that specific session. `useOscarLLPPartners` reshapes from singular-binding hook to a `listPartnerStates()` × `listSessions()` join returning `OscarLLPSessionRow[{id, label, name, created_at, updated_at}]` sorted by `updated_at DESC`; subscribes to SESSION_CREATED|DELETED|RENAMED|FORKED for live refresh. `main.ts` IPC: `bind-session` accepts optional label and PREPENDS to `sessions[]` (dedupe-by-id); new `unbind-session` reserved for future "delete partner session" UI; `writeOscarLlpRegistry` upgraded to `.tmp + fs.rename` atomic-write floor. Label fallback chain: partners.json explicit label → goosed `Session.name` (auto-emitted, first-message-derived) → `Session <date>`. `formatRelative` ported inline from `ChatHistorySearch.tsx:41-54` (just now / Nm / Nh / Nd / Mon D — CLAUDE.md "no premature abstraction"). New `.oscar__llp-*` CSS family for the inline session block (subordinate visual weight to partner header, mirroring Sprint 19b matter-row treatment). Verification: tsc baseline 17 errors pre AND post (zero net new); eslint clean on touched files; `test-oscar-llp-agents.js` 3/3 PASS on real MiniMax (composition seam unbroken — Sprint 27 doesn't touch recipe builder / prompts / verification gate). v1 fixture verification dropped per drop-order #2 (no live host has v1 partners.json on lq-vps; Arturs's Crostini dogfood validates if v1 data exists there). One ADR (092). No Rust core touch. No new sibling MCP. No `main` rebase. Commits: `e63f15fb7` (ADR-092 standalone) → `09205b9f4` (code) → `d2252930f` (close-out). | Closed 2026-05-22 on code; Crostini dogfood → 27b if needed |
| 26 | Back-port Sprint 25 findings to production `verificationGateBlock.ts` + targeted constraint relax for partner-training-bias defect. Single commit `d9f6af68c` applies P1 (-62 chars, fetch parenthetical), P2 (-31 chars, doc-text precondition), P3 (-425 chars, pre-scripted escalation reply template + follow-up paragraph; keeps abstract escalation directive at line 27) AND inserts one constructive acknowledgement sentence (~+280 chars) stating that document text supplied directly in the user message — pasted inline or under `## Document context` heading — is authoritative source material equivalent to `oscar-document-reader` output. Net -284 bytes, 29 lines (was 33). All 10 partners pick up change byte-for-byte via [[ADR-081]] composition seam. [[ADR-090]] (decision-time, 40 lines) captures the targeted constraint relaxation: Sprint 25's ~240 partner-runs of evidence justify the exception for partner-training-bias defects, not a general license to add. **Sarah Phase 4 validation** (re-ran iter-0 vs Sprint 25 baseline preserved on disk at `iter-0-sprint25-baseline/`): TIMEOUT 45→0%, NO_ANALYSIS 25→0%, DELIVERED 30→95%, total output 30→100% — both Sprint 25 exit criteria met by 4-7×; Sprint 25's best Sarah cycle (iter-3, 60% delivered) substantially exceeded in one Sprint 26 cycle. **Marcus Webb Phase 5 cross-partner transferability** (non-trio empirical A/B via `git show HEAD~2:...verificationGateBlock.ts` → file → 8s sleep → restore pattern): pre-back-port 0% timeout, 20% NO_ANALYSIS, 5% PARTIAL, 75% delivered → **post-back-port 0% timeout, 0% NO_ANALYSIS, 0% PARTIAL, 100% delivered**; all 5 problem instances pre (4 NO_ANALYSIS + 1 P3-script-escalation) flipped to DELIVERED post. Cross-partner transferability claim per [[ADR-081]] §Hybrid 2 empirically validated for one of seven non-trio partners; structural inference covers remaining six. Substrate extension to support Marcus: three additive entries in `evals/oscar-llp/scripts/{lib-benchmarks.js, lib-recipe24.js, run-partner-cycle.js}` (Marcus → cuad-saas mapping + Commercial Contracts metadata, ~17 LOC, commit `dc5feaa30`). **Sprint 25's negative finding refuted**: the targeted acknowledgement sentence — one sentence, ~280 chars — closes the partner-training-bias-on-MCP-fetch NO_ANALYSIS residual entirely on both Sarah (30→0%) and Marcus (20→0%); the constructive addition does load-bearing work subtraction alone could not. Sprint 22 smoke `test-oscar-llp-agents.js` 2/3 PASS pre-change (Sarah+Helena; Aisha skips verification-pass on focused-tool questions — pre-existing MiniMax non-determinism, test uses hardcoded `VERIFICATION_DIRECTIVE` independent of `verificationGateBlock.ts`); 3/3 PASS post-change — composition seam intact. Total spend $0.87 (9% of $10/PCM dev-key cap; far under $3 plan estimate because back-port reduces tool-hunting wall-clock). One ADR (090). No Rust core touch. No new sibling MCP. Pushed: `lavern-firm-mode` SHAs `d9f6af68c` → `dc5feaa30` → `6476990b6`. | Closed 2026-05-22 on code |
| 25 | Execute Sprint 24-C iteration (interactive, Max-only). Reworked Sprint 24-C's SDK-automated iteration harness into a Claude-Code-interactive shape per [[ADR-082]] (decision-time, pre-trim — "via Max subscription" means Claude Code interactive, not @anthropic-ai/sdk). Phase 0: deleted `lib-claude.js` (217 LOC SDK wrapper) + `package.json` (only dep was `@anthropic-ai/sdk@0.40.1`); renamed `run-iteration.js` → `run-partner-cycle.js` (394 → 197 LOC, Phase A only — judging moves to in-conversation Phase B); new `apply-proposal.js` (88 LOC Phase C subtractive applier); trimmed `lib-cost-log.js` (MiniMax-only); rewrote README + NOTICE.benchmarks.md; net Phase 0 diff −452 LOC. Phase 1: new `loaders/maud-loader.js` (168 LOC, MAUD CSV from Atticus Project → 50 instances) + `loaders/cuad-loader.js` (201 LOC, CUADv1.json → 50 instances per privacy/saas filter); **CUAD privacy-clause substitution** — CUAD does NOT include "Data_Privacy" in its 41-clause taxonomy; Diana's filter substitutes data-flow + third-party-access commercial clauses (Audit Rights / Affiliate License / Anti-Assignment / Insurance / Change Of Control / Covenant Not To Sue) as closest privacy-adjacent surface CUAD offers; flagged in partner question + NOTICE. `sanity-check.js` `--docs all` wrapper bug fixed (Sprint 23 runner doesn't recognize `all` literal; default is already all 3 docs). Phase 2: sanity check FAILED ±2pp gate (+4.3pp vs Sprint 23 -3.8pp baseline; drift 8.1pp); **waived** — diagnosed as Sprint 23's N=6 CIs of [13.8%, 57.3%] and [20.1%, 66.8%] overlapping wildly; substrate diffs since 24-A were cosmetic only (frozen per ADR-077); gate over-tuned to N=6 noise. Phase 3: 12 partner-cycles executed (3 partners × 4 cycles × 20 instances = 240 partner runs + 6 sanity = 246 total). **Sarah Chen (MAUD)**: 30% → 60% delivered (+30pp); timeouts 45 → 10% monotonic; 860 chars cut (-12.9%); MiniMax $0.897. **Diana Park (CUAD-privacy)**: 65% → 80% total output; **iter-2 REGRESSED -20pp** after the same Phase 4+5 cut that helped Sarah (Diana's Phase 4 Consent Architecture + Phase 5 Produce Deliverables provided implicit "I have a method" scaffolding — removing it caused fallback to "I need document first"); iter-3 doc-text edit recovered + improved; 1066 chars cut; MiniMax $0.965. **Aisha Khan (CUAD-saas)**: 80% → **95%** total output (highest result of any partner-cycle in Sprint 25); Phase 4+5 cut **deliberately skipped** because Aisha's Phase 4 (Licensing/IP Analysis) + Phase 5 (Vendor Risk Assessment) are core to tech-tx (not off-topic ceremony); escalation-script removal (-425 chars) produced +25pp single-cycle delivery improvement (biggest of sprint); 518 chars cut; MiniMax $1.020. Phase 4 cross-partner extraction: 4 patterns + 1 negative finding. **P1** (fetch-via parenthetical, -62 chars) and **P2** (doc-text precondition, -31 chars) rated HIGH transferability to all 7 non-trio partners — recommended back-port into production `verificationGateBlock.ts` (Sprint 26+ ADR). **P3** (escalation script, -425 chars) rated MEDIUM. **P4** captures "transferability requires content inspection" finding from Diana iter-2 regression. **Negative finding**: partner-training-bias on MCP-fetch is beyond subtractive reach — residual 5-30% NO_ANALYSIS rate held across all cycles; closing the gap requires a constructive addition (violates Sprint 25's subtractive-only constraint); Sprint 26 should consider relaxing for partner-training-bias defects. Substrate that survives unchanged: `lib-recipe24.js`, `lib-subtractive.js`, `lib-benchmarks.js`, `sanity-check.js`, prompts/*. One ADR (082, decision-time, 40 lines). Total live spend $3.18 MiniMax (32% of $10/PCM dev-key cap); $0.00 Anthropic (judging in-conversation under Max subscription — 97% cost reduction vs Sprint 24-C's ~$52 SDK envelope). No Rust core touch. No new sibling MCP. Pushed: `lavern-firm-mode` SHAs `583950b80` → `0f9b1b5c0` → `1b611c736` → `91d2eba84` → `0205e729e`. | Closed 2026-05-22 on code |
| 24-B/C | Combined sprint: Lavern Pipeline port + cross-partner iteration eval harness. (24-B) Watchman → Reader → Curator ported as Goose parent recipe + 3 sub-recipes per [[ADR-079]]; Watchman (lifted from `claw/watchman.ts:39-82`) classifies + routes; Reader (lifted from `claw/local-analysis.ts` + `reader-templates.ts:65-191`) per-clause analysis + synthesis using doc-type templates; Curator ships as 20-line STUB (substantive heartbeat-driven Curator deferred to Sprint 25 per ADR-079 §Consequences). Doc-head ingress via new `read_document_head` tool on `oscar-document-reader-mcp` (sibling repo commit `ba283bf0d82ac3f88b2b61c6bbbc1e017845eb74`); precedent-board persistence reuses `oscar-baselines-mcp` at per-user-per-area scope `~/.config/oscar/state/lavern/precedents/` per [[ADR-080]] (CLAUDE.md reuse-over-rebuild — no new MCP). UI: `LavernPipelineView.tsx` doc picker at `/oscar-llp/pipeline`; card above 10-partner roster (firm-level work, not separate sidebar group); new `oscar:llp:pipeline:{ensure-dir,list-recent-docs}` IPCs. Parent recipe constructed programmatically by `buildLavernPipelineRecipe.ts` (mirrors Sprint 22 partner-recipe pattern); 3 sub-recipe YAMLs + 1 reference parent YAML ship to `ui/desktop/sub-recipes/`; bundler picks them up via existing `prepareSubRecipes()` zero-edit. `test-lavern-pipeline.js` `--parse-only` 4/4 PARSE OK. (24-C) Hybrid 2 prompt architecture per [[ADR-081]] — byte-identical 21-line verification gate (sha256 `1f67d064f6987ba1`, verified across all 10 partners) extracted into `verificationGateBlock.ts`; `buildOscarLLPPartnerRecipe.ts:123-127` composes as 4th array entry alongside identity/company/partner-body. Per-partner files shrink 22 lines each (gate + preceding blank); 1297 → 1097 lines + 25-line shared module. Untouched: `prompts/raw/*.ts.original` Apache 2.0 verbatim per [[ADR-072]] + [[ADR-078]]; `evals/lavern-jv/scripts/lib-recipe.js` SPRINT_22_DIRECTIVE + RALPH_DIRECTIVE frozen per [[ADR-077]]. New `evals/oscar-llp/` directory: 8 scripts (orchestrator + sanity-check gate + Anthropic SDK wrapper with prompt caching + per-iteration partner-recipe builder + MAUD/CUAD/LegalBench format adapters + Layer-B subtractive-edit validator + trajectory + Phase 2 extractor + cost log), 3 prompts (Layer-A subtractive constraint, per-partner judge rubric, Phase 2 cross-partner extractor), 5 benchmark stub files with `_schema` documentation, `package.json` pinning `@anthropic-ai/sdk@0.40.1`, NOTICE.benchmarks.md (Atticus + Stanford LegalBench). Trio: Sarah Chen (M&A, MAUD) / **Diana Park (Privacy, CUAD-privacy + LegalBench)** / Aisha Khan (Tech Tx, CUAD-SaaS). Helena Voss NOT in trio — Sprint 22 substrate inline-cast as Privacy was a bug (production prompt has her as Tax); +33pp Helena × Doc 1 win on Sprint 23 with-Ralph was on that Privacy inline-cast, closer to Diana than to real Helena Tax — swap preserves methodology-level continuity. Cost envelope estimate ~$52 (Anthropic ~$15 with caching + MiniMax ~$36 + sanity check + Phase 2) against brief's $60-100. Live iteration runs deferred to user invocation (require pay-as-you-go Anthropic API key — Max subscriptions don't issue API keys; flagged as pre-execution gate). Three ADRs at decision time: 079 (pipeline shape), 080 (precedent persistence), 081 (Hybrid 2). Pushed: `sarturko-maker/goose` `lavern-firm-mode` SHAs `f300b0f09` → `e4febabea` → `fb2952971` → `604131ac9`; sibling `sarturko-maker/oscar-document-reader-mcp` `main` SHA `ba283bf0d82ac3f88b2b61c6bbbc1e017845eb74`. No Rust core touch. No new sibling MCP (reuse mandate). | Closed 2026-05-21 on code; iteration runs → user invocation |
| 24-A | Lavern → Oscar LLP rebrand. 10-partner sidebar bench reframed from Sprint 21 placeholder "Lavern" to in-house-standard "Oscar LLP" partnership shape; "Lavern" reserved for Sprint 24-B+C's pipeline port (Watchman → Reader → Curator). Single atomic mechanical sweep (31 files): `ui/desktop/src/components/oscar/lavern/` → `oscar-llp/`; symbols `LAVERN_PARTNERS → OSCAR_LLP_PARTNERS` / `LavernPartner → OscarLLPPartner` / `buildLavernPartnerRecipe → buildOscarLLPPartnerRecipe` / `LavernRoster → OscarLLPRoster` / `useLavernPartners → useOscarLLPPartners` / `readLavernRegistry → readOscarLlpRegistry` / `lavernWorkingDir → oscarLlpWorkingDir` etc.; IPC `oscar:lavern:* → oscar:llp:*` (renderer `window.electron.llp.*`); state file `~/.config/oscar/state/lavern/partners.json → .../oscar-llp/partners.json`; working dir `~/Documents/Oscar GC/Lavern/<slug>/ → ~/Documents/Oscar GC/Oscar LLP/<slug>/`. Read-time lazy `fs.rename` migration mirrors ADR-047 + ADR-032; POSIX-atomic, idempotent, interrupt-safe, carries `.goose/memory/` with the working dir. ADR-029 trust-bypass widens to three-way OR (`Oscar GC` \|\| `Lavern —` \|\| `Oscar LLP —`); Sprint 25 cleanup drops `Lavern —` recognition. Apache 2.0 attribution verbatim per §6 — `prompts/raw/*.ts.original` (10 files) and `evals/lavern-jv/` directory untouched, NOTICE body unchanged (only per-section heading reframes + path refs). 10 partner prompt bodies: `at Lavern` → `at Oscar LLP` only (raws + provenance comments + `The Shem → Lavern firm rename` lineage stay). Test scripts renamed: `test-lavern-{agents,revision}.js → test-oscar-llp-{agents,revision}.js`. `evals/lavern-jv/scripts/lib-recipe.js` partner-roster firm strings only (directory + filenames + frozen `SPRINT_22_DIRECTIVE`/`RALPH_DIRECTIVE` constants stay per ADR-077). One ADR (078, 44 lines). 5 commits. Regression tests on real MiniMax: 3/3 test-oscar-llp-agents PASS (~$0.04); test-oscar-llp-revision c3 PASS via path B (~$0.02); evals/lavern-jv 1-tuple spot-check confirms `Oscar LLP — Sarah Chen` recipe title (~$0.07). Total spend ~$0.13 (under $0.20 brief envelope). No Rust core touch; no sibling repo modified. | Closed 2026-05-21 |
| 23 | Ralph Loop gate-and-revise (Shape A — prompt-borne) wired into all 10 Lavern partners + Lavern-baselined eval harness exercising it. Sprint 22's "Verification before delivery" paragraph REPLACED (not appended) in every partner prompt by a single Ralph Loop paragraph mandating revision on literal `## Verification Pass: ISSUES` marker; verbatim-quote of the verification-pass header makes compliance transcript-auditable; 2-revision budget enumerated as concrete sequence steps; exact escalation phrase. `Recipe.settings.max_turns = 12` substrate safety ceiling wired into production `buildLavernPartnerRecipe.ts` + Sprint 22 test substrate. **Architectural call (ADR-076)**: Shape A — substrate evidence forces it. `delegate()` returns plain text (`summon.rs`); `run_subagent_task()` returns `Result<String, anyhow::Error>` (`subagent_handler.rs:48`); `Recipe.retry: Option<RetryConfig>` is shell-failure semantics, not semantic revision. Shape B would cost ~100-150 LOC of Rust core across `summon.rs` + `agent.rs` + Recipe schema — violates CLAUDE.md "do not modify the Rust core unless absolutely necessary" with no Sprint-23 evidence demanding it. New dogfood test `scripts/test-lavern-revision.js` (single-partner Sarah Chen × deliberately-ungrounded "section 99.9" prompt) characterized over 5 runs: 4/5 PASS (paths A/B — explicit acknowledgment or silent neutral substitution); 1/5 FAIL (path X — fabricated "## Section 99.9 Verbatim" header). New `evals/lavern-jv/` directory (Apache 2.0, Lavern SHA `7c2efe61524b14c632bee8f14d9bbcbdd85d0cfd`): 3 CUAD JV contracts + verbatim 28-item RUBRIC + adapted rubric (drops Watchman/pipeline metrics; adds 4 Oscar-GC global axes) + judge prompt + 4-file runner (845 LOC, each file under 300-line cap). Sprint 22 verification directive frozen at SHA `08a5381a7` as `SPRINT_22_DIRECTIVE` constant for reproducible without-Ralph A/B baseline. **Full sweep: 3 partners × 3 docs × 2 configs = 18 partner runs + 18 batched judge calls on real MiniMax-M2.5; 14/18 partner runs succeeded (4 with-Ralph timeouts at 300s ceiling); 17/18 judge calls succeeded (1 PARSE_FAILED on "MISSING"/"MISSED" synonym); wall-clock 2680s; cost ~$1.40 against $10/PCM dev-key.** **Substantive result: Δ_grounded = -3.8pp (NEGATIVE) on grounding-touched rubric items** (with-Ralph 33.3% vs without-Ralph 37.1%; bootstrap 95% CIs overlap heavily); mean `grounded_citations` with-Ralph 0.71 vs without-Ralph 0.96 (-0.25); mean `hallucination_count` 0.00 both configs. Per ADR-076's exit-criterion framing, Sprint 24 reopens the Shape A vs Shape B architectural call with this data; three Sprint 24 options sketched in `evals/lavern-jv/reports/sprint-23-baseline.md` (Shape B substrate-borne; Shape A-tightened; hybrid with extended timeout + selective ISSUES). Honest-scope drop-order untouched (full default scope ran). Two ADRs (076 Ralph Loop, 077 eval baseline + judge + A/B). No Rust core touch; no sibling MCP repo modified; only `sarturko-maker/goose` touched this sprint. | Closed 2026-05-20 on code; Δ_grounded < 0 → Sprint 24 reconsiders shape |
| 22 | Lavern Tier-A MCPs lifted as Goose stdio extensions + `verification-pass` sub-recipe wired into every partner recipe. Six new sibling MCP repos (`oscar-knowledge-base-mcp`, `oscar-document-reader-mcp`, `oscar-risk-pricing-mcp`, `oscar-baselines-mcp`, `oscar-grounding-verifier-mcp`, `oscar-document-checks-mcp`) lifted/adapted from Lavern (Apache 2.0, commit `7c2efe61524b`) — three clean lifts, two adapters (decoupled from Lavern's `session.debate.findings` / `SessionReportCard`), one net-new (risk-pricing — Lavern's source is orchestration scaffolding, not substantive pricing). All on `@modelcontextprotocol/sdk` 1.29.0; raw Lavern sources preserved at `src/_lavern-original/` per Sprint 21 ADR-072. **Path A** architectural call (ADR-074): per-partner recipes declare their own MCP loadout — forced by Goose substrate (SubRecipe has no extensions override at `crates/goose/src/recipe/mod.rs:119-129`; agent files accept only `name`/`description`/`model` at `summon.rs:90-97`; subagents inherit parent extensions, filterable not extendable). Sprint 24's Project recipe can wrap partner recipes as `sub_recipes` without disturbing Sprint 22's output. `verification-pass.yaml` sub-recipe bundled at `<resources>/sub-recipes/`; `Recipe.sub_recipes` declaration auto-injects summon (`recipe/mod.rs:255-271`) so partners get `delegate()` for free. `prepare-oscar-bundle.js` extended with six SIBLING_MCPS + smoke-test entries + a `prepareSubRecipes()` step; `BUNDLE.json` manifest gains a `sub_recipes` block. All 10 partner prompts gained one appended verification paragraph (raws untouched). Two test harnesses per CLAUDE.md ("Pipeline tests must NOT mock LLM calls"): `scripts/test-verification-pass.js` (no LLM — uses `goose run --explain` to validate the recipe loader resolves both verification-pass.yaml standalone and a parent with sub_recipes pointing at it); `scripts/test-lavern-agents.js` (**real MiniMax** invocations on three representative partners — Sarah Chen / M&A, Helena Voss / Privacy, Aisha Khan / Litigation; transcripts persisted; 3/3 pass after directive sharpening). Three corrections to ADR-073 captured in ADR-075: `legal-md-compiler` dropped (memory-system import is Tier B/C); `document-checks` + `grounding-verifier` + `baselines` need adapter work; **`evals/jv/` is NOT empty** as ADR-073:71 claimed — has RUBRIC + 3 CUAD JV contracts + scored reports; Sprint 23 should lift not build. Adversarial-pass sub-recipe dropped per brief's honest-scope cut. Two ADRs (074 Path A architectural, 075 lift policy + corrections). Reference repo: `/srv/projects/lavern/` (Apache 2.0, pinned `7c2efe61524b14c632bee8f14d9bbcbdd85d0cfd`). | Closed 2026-05-20 on code |
| 21 | Lavern firm-mode — multi-partner consult inside Oscar GC. New sidebar group between Quick chats and Practice areas; `/lavern` roster of 10 invented partners `[Name] ([Specialism])` lifted-and-adapted from `github.com/AnttiHero/lavern` (Apache 2.0, HEAD `7c2efe61524b`): Sarah Chen (M&A), Marcus Webb (Commercial Contracts), Daniel Reeves (Litigation), Priya Patel (Employment), James Okafor (IP), Helena Voss (Tax), Diana Park (Privacy), Robert Sinclair (Capital Markets), Aisha Khan (Tech Transactions), Thomas Schmidt (Regulatory). Each partner is a recipe with per-partner working_dir under `~/Documents/Oscar GC/Lavern/<slug>/` — Goose Memory's `agent-working-dir` meta scoping gives automatic per-partner isolation (no code needed). Identity cascade via new `userIdentityBlock.ts` (Lavern-only this sprint; same gap in practice-area recipes carried forward). Partner→session binding state file `~/.config/oscar/state/lavern/partners.json` for resume-on-existing. Trust-bypass (ADR-029) widened to recognize `Lavern —` title prefix. Prompt adaptation mechanical at vendor-time (ADR-072): `The Shem` → `Lavern`, debate-board / output-format JSON / personality numerics stripped, Memory Protocol rewritten to Goose Memory, first-line rename to include partner name. Raw originals checked in at `prompts/raw/<slug>.ts.original`. Sprint 22 architecture committed in ADR-073 — Tier-A 7 portable MCPs to lift as stdio (knowledge-base, baselines, document-reader→reuse adeu, grounding-verifier, document-checks, risk-pricing, legal-md-compiler), Tier-B 5 concepts to map onto Goose primitives (evaluator-gate → pre-delivery `SubRecipe`, debate-board → ad-hoc sub-recipe, handoff → `SubRecipe`, approval-gate → UI affordance, workflow-engine → recipe templates), Tier-C 8 internal mechanisms either covered or deferred. Sprint 23 (per-partner eval harness) confirmed needed because Lavern's `evals/` directory is empty. 8 incidental pre-existing ESLint `no-console` directive cleanups (4 files) needed to pass `pnpm lint:check`. Three ADRs (071 structural, 072 adaptation policy, 073 MCPs deferred + Goose-shape translation table). Crostini dogfood passed on `oscar-gc-sprint21` draft release; three findings carry to Sprint 24 (per-partner conversation history; @-tag partners from chat via `ChatInput.tsx` existing mention infra + Goose `summon` platform extension; multi-agent project collaboration via `Recipe.sub_recipes` + `subagent_handler`). | Closed 2026-05-20 |
| 20-M2 | M2 of the right-panel master brief — section registry + per-area composition. Turns M1's inert pane body into a data-driven stack: `PanelSectionId` as a closed 8-member literal union (`MatterFacts \| ProgrammeFacts \| Skills \| Playbooks \| Redlining \| Forum \| Deadlines \| History`); `PracticeAreaShape.defaultPanelSections: PanelSectionId[]` required on all 13 areas; `area_overrides.panel_sections` narrows from M0's `string[]`. Resolution = `override ?? shape default ?? Forge fall-through` (entry_noun → trio). `sectionRegistry` ID→ComponentType map (M2 maps every ID to `PanelSectionStub`; M3+ swap entries without `RightPaneShell` change). Default composition confirmed via plan-mode AskUserQuestion — Commercial gets [MatterFacts, Skills, Playbooks, Redlining]; Privacy/Regulatory/AI-Governance get [ProgrammeFacts, Skills, Playbooks, Deadlines]; the four Disputes areas get +Forum; trio-only for corporate/employment/ip/product. History stays in the enum but absent from defaults — M3 decides. New `useActiveAreaSections` pure-derivation hook; `useRightPaneVisibility` extended to expose `areaId` from the existing `matters.lookupSession` response (no second IPC). `OscarUserProfilePracticeArea.entry_noun?` typed for Forge fall-through (Sprint 19 P4 already writes this on Forge-created entries). Visual harness on lq-vps: `capture-m2.js` + `capture-m2.sh` drive 5 states under Xvfb (3 area defaults / collapsed-rail / Commercial overridden via mid-run profile.json mutation); PNGs at `docs/screenshots/sprint-m2/`. Section IDs asserted in-flight per state. pnpm 11 `verify-deps-before-run` quirk worked around by invoking `./node_modules/.bin/{tsc,electron-forge}` directly (Sprint 17b pnpm-friction surface). No Rust touch, no new IPCs, no sibling-repo changes. One ADR (070 panel sections declarative + per-area defaults — 37 lines). | Closed 2026-05-20 on code; per-sprint Crostini dogfood deferred to M8 per Arturs's doctrine |
| 20-M1 | M1 of the right-panel master brief — docked right pane shell (empty body). Lands the structural primitive so M2's section registry and M3+'s real bodies have a mount point. Brief-vs-reality drift surfaced: matter chats live at `/pair?resumeSessionId=…` (NOT `/practice/:areaId?resumeSessionId=…` as the brief read); fix is `matters.lookupSession()` — the existing primitive `MatterBackButton.tsx:29` already uses (decided via AskUserQuestion). Mirror sidebar drag/persist with `direction: -1` (LEFT-drag widens), absolute-width persistence (no CONDENSED baseline), single sticky boolean `isRightPaneExpanded: boolean \| null` (null = honor route default — on in matter, off in quick-chat — explicit boolean sticks). Two new Settings keys (`rightPaneWidth`, `isRightPaneExpanded`); NavigationContext extended; new `rightPane/` module (RightPaneShell with "Loadout" eyebrow + chevron toggle, `useRightPaneVisibility` hook); second drag system in AppLayout parallel to the sidebar's; `.oscar__right-pane*` editorial CSS family. Quick-chat default renders as 32px chevron rail rather than fully-absent pane — matches master plan's "chevron toggles either way" framing. Visual verification on lq-vps: `ui/desktop/scripts/capture-m1.js` + `scripts/capture-m1.sh` drive 7 states via Playwright/CDP under Xvfb (Hub / matter open default / drag wide / drag narrow / quick-chat / collapse via chevron / restart + persistence); PNGs at `docs/screenshots/sprint-m1/`. Drag math verified in-flight (320→480→240 confirmed via boundingBox). Toggle click via direct DOM `.click()` to bypass extension-load toast's z-stack. Harness also revealed latent QuickChatButton "STARTING…" stuck-state bug (fork-hygiene carry; not in M1 scope). No Rust touch, no new IPCs, no sibling-repo changes. One ADR (069 right pane layout primitive — 36 lines). | Closed 2026-05-20 on code; per-sprint Crostini dogfood deferred to M8 per Arturs's doctrine |
| 17b | Crostini dogfood patches — got Sprint 17 launching after 4 build cycles. (1) pnpm 11+ silently dropped React version pin from `pnpm.overrides` in package.json; moved into `pnpm-workspace.yaml` (commit `46642a0c9`). (2) pnpm 11's hybrid linker laid down 1094 real dirs at `ui/node_modules/<name>/` AND a symlink at `ui/desktop/node_modules/react` into the .pnpm store → Vite bundled two copies of `react@19.2.4` into the same chunk → `Cannot read properties of null (reading 'useRef')` from dispatcher mismatch. Fix: `resolve.dedupe: ['react', 'react-dom', 'react/jsx-runtime', 'react/jsx-dev-runtime']` in `vite.renderer.config.mts` (commit `9845e4f37`). (3) Dogfood with the launching build surfaced three findings — F1: P5 wired Ironclad/DocuSign into the recipe → matter open hit upstream Goose's MCP-OAuth which Ironclad rejects ("not a trusted client" — Goose's client_id isn't registered with these SaaS vendors). F2: top-level Integrations dropdown listed all 13 catalog areas instead of the user's selected practice areas. F3: Tavily silent during intake's regulatory hypothesis — confirmed by `grep -c tavily` on `llm_request.N.jsonl` that the extension loaded and exposed `tavily-search`; MiniMax-M2.5's tool-choice, not a wire-through bug. F1 + F2 fixed in commit `3209ebb5d`: paid-wrapper visible-only state ("Subscription — not yet installable"); target dropdown filtered to `usePracticeAreas()` × entry's `relevant_areas`; defence-in-depth in `buildExtensionFromIntegration` skips paid wrappers at recipe-build even if a stale state file carries one. F3 carries to Sprint 18 (prompt lever — "force-cite ≥2 dimensions"). Lawyer-dogfood E1/E3/E4/E5 validation continues asynchronously on the draft release. No new ADRs; ADRs 059–062 stand. | Closed 2026-05-20 on code; E1/E3/E4/E5 validation continues |
| 17 | Integrations (see and add) — first transparent in-house catalog of MCPs. `INTEGRATIONS_OVERLAY` over `.mcp.json` vendor data; three trust tiers (bundled / trusted / community); community-tier "Add and acknowledge" with hostname + license + subscription + maintainer + egress copy. Per-area Integrations tab on every practice area + top-level Integrations sidebar entry next to Forge (ADR-039 untouched). Sibling state file `~/.config/oscar/state/<area-id>/installed_integrations.json` (profile.json stays single-writer per plan-mode AskUserQuestion). 6-entry seed: `oscar-fs` (bundled, Always-on), `CourtListener` (trusted, free, IP + 4 disputes areas), `Slack` / `Google Drive` (community, cross-area), `Ironclad` / `DocuSign` (community, paid, Commercial). Recipe-builder merge in `MattersLanding.openMatter` threads installed entries into `extraExtensions`; new `ensureRecipeSecrets.ts` helper gates spawn only when an installed entry's env_keys is genuinely missing (no modal flash for the no-op case). Naming flagged in plan-mode: "marketplace" → "Integrations" (lawyer-natural; corporate-IT vocabulary; brief explicitly reserves "operator marketplace" for a future product). Brief's OpenContracts / Open Legal Compliance MCP / US Legal MCP seed dropped — zero references in `/srv/projects/goose`. Four ADRs (059, 060, 061, 062 — amends 042). Crostini E1–E5 dogfood carries to Sprint 17b. | Closed 2026-05-19 on code; runtime gates → 17b |
| 16 | Re-sequenced intake (practice scope before regulatory hypothesis) + Goose-native Tavily key surface. Corrects two structural Sprint 15 P8 dogfood findings: regulatory hypothesis fired before practice areas were known (privacy-stack default missed REACH/WEEE/RoHS for industrial distribution), and Tavily key entry required terminal commands. systemPrompt.ts rewritten under new P1–P9 numbering — practice scope (P4) precedes the regulatory hypothesis (P5); new Tavily query template substitutes practice-area scope at inference; data-protection-always guardrail in P5. ADR-055 (intake re-sequence, amends ADR-050). Eval rubric rework — new `regulatory-fit` judge axis keyed off per-persona `regulatory_answer_key` (`load-bearing`/`nice-to-have` tiers); coverage.md deconflicted so industry-fit isn't double-penalised; new pass criterion adds per-persona regulatory-fit floor ≥3.0 (catches Sprint 15-shape failures the mean averaged out). 6 personas augmented + new 7th `arturs-industrial-eu.json` mirrors Arturs's P8 dogfood scenario for the synthetic eval. ADR-056 (regulatory-fit axis + answer-keys, amends ADR-054). Anchor 2 = B1: `discover_recipe_secrets` lifted from `goose-cli/src/recipes/` into `goose/src/recipe/` (CLI keeps working via re-export shim); new `POST /recipes/scan_secrets` route on goose-server (registered in openapi.rs; TS SDK regenerated); new generic `RecipeSecretsModal.tsx` scans the recipe + prompts for any unset `env_keys` via `upsertConfig(is_secret:true)`; wired into `OscarOnboardingGuard`. `buildTavilyExtension()` becomes parameter-less — declares `env_keys: ['TAVILY_API_KEY']` + `${TAVILY_API_KEY}` URI substitution; `merge_environments` + `substitute_env_vars` at session-spawn resolve from env-then-keyring (`Config::get_secret`). `resolveTavilyKey.ts`, `oscar:resolve-tavily-key` IPC, `oscarResolveTavilyKey` preload bridge, `~/.config/oscar/secrets/tavily.json` reader all deleted. INSTALL_CROSTINI.md gains Tavily as Step 7 in First Launch. ADR-057 (env_keys + keyring, amends ADR-052), ADR-058 (secret_discovery lift to goose-core — closes Goose's own docs-vs-code gap, upstream-PR-worthy). Carries 1+2 (visible Tavily provenance, recap copula bug fix) shipped in Phase 1 systemPrompt.ts. Carry 4 — ADR-048 written (Adeu MCP App diff preview design from Sprint 14 plan); implementation deferred to Sprint 16b. **Scope honesty**: iter-3 eval (needs paid MiniMax + Tavily API) and clean-Crostini dogfood (E1–E4 exit criteria) carry as runtime gates to Sprint 16b. Carry 3 (platform-extension trim) carries to Sprint 17 — Goose's Settings struct has no per-recipe disable mechanism; needs Rust touch or runtime experiment. Five ADRs (048 written, 055, 056, 057, 058). | Closed 2026-05-19 on code; runtime gates → 16b |
| 15 | Practice-context intake: in-house shape, agentic, model-eval'd. Displaces Sprint 14's reserved Sprint 15 candidate (adeu preview UI → Sprint 16; ADR-048 stays reserved). Rewrites the onboarding rule-set so an in-house lawyer can describe their practice in ≤5 minutes and downstream practice-area agents are *briefed at turn 1*. ADR-050 (intake rule-set, 8 rules: budget, signal-density branching, batch aggressively, hypothesis-confirm via Tavily, always-open final question, P3.5 skip-when-covered, hard stops, state-tracking). Schema v3 (ADR-051) with `company_context` block — industry depth, geography, regulatory_baseline + provenance, recurring_matters, stakeholders, risk_appetite, open_notes; v2 profiles route to gated re-intake via `captured_via="needs-re-intake"` sentinel. Tavily as hosted streamable_http web-search (ADR-052, amends ADR-042 egress); user-provided runtime key. Recipe-time `company_context` injection (ADR-053): `companyContextBlock.ts` prepends `## About this company` dense markdown block to practice-area `instructions`. Eval harness (ADR-054) — six axis-covering personas, three judge prompts, CLI orchestrator routing every LLM call through `goose run`. **P6 ran live across two iterations (12 persona runs, ~25 min/iteration, $10/month MiniMax dev cap)**: iter-1 (`fc5e756eb`) coverage 4.83 / efficiency 4.20 / downstream 2.58 → iter-2 (`98cc2f73b`) coverage 4.50 / efficiency 4.80 / downstream 2.92 after explicit "Use the About this company block actively" prompt fix. Intake itself works; downstream-briefing 2.92 below 4.0 target. **Stage 2 (P8) carries to Sprint 16** — Arturs's qualitative dogfood is the right next signal; iter-3 prompt levers documented in `docs/sprint-15/self-assessment.md`. Five ADRs (050, 051, 052, 053, 054). | Closed 2026-05-19 on Stage 1 (two live iterations); Stage 2 → 16 |
| 13 | Lawyer-shape redline: word-diff in adeu + prompt discipline. Closes the "adeu doesn't redline like a lawyer" carry-forward from Sprint 10 / Sprint 12. adeu vendor-patch (ADR-045) wires its existing `generate_edits_from_text` (word-level diff via diff-match-patch in `adeu/diff.py:189`) onto the `process_document_batch` MODIFICATION branch in `engine.py:_pre_resolve_heuristic_edit` — the algorithm existed in adeu but was only invoked on `diff_docx_files`. Patch committed at `docs/redline/adeu-1.6.9-batch-path-word-diff.patch` (51 lines), bundled by `prepare-oscar-bundle.js`, applied by `postinst.sh` after `pip install adeu==1.6.9`. Commercial recipe prompt discipline (ADR-046): preserve list + anchor-preservation idiom + qualifier/mandatory-law-catch-out failure-mode examples + consequence framing inlined into `systemPrompt.ts`. New `docs/redline/lawyer-shape-criteria.md` formalises six numbered criteria (OOXML granularity, preserve discipline, cross-clause consistency, no emphasis-Markdown, author propagation, coherence verification) — the user-sign-off contract. Phase 5 verification harness (`scripts/dogfood/verify-redline-shape.py` + deterministic Sprint 9 batch replay): pre-patch baseline 15 tracked elements / median wrap 60 words → post-patch 68 elements / median 2.5 words; max wrap Δ -67 words; PASS on §1 hard gates. Phase 6 .deb built on lq-vps via `scripts/build-oscar-deb.sh`, uploaded to draft release `oscar-gc-sprint13`. Three latent Sprint 12 bugs surfaced + fixed during the build/dogfood: lockfile drift (ADR-040 dep not in lockfile), `prepareVendoredMcps` mis-resolution + top-level-await format, and the dogfood-surfaced duplicate-shebang regression in vendored ESM bundles. Phase 7 Crostini dogfood passed; five UX findings carry forward to Sprint 14 (load-bearing anchor: in-house framing for per-area matter dialogs). Two ADRs (045, 046). | Closed 2026-05-19 |

See `SPRINT_LOG.md` for entries. See `CLAUDE.md` for operating rules.

## Branding follow-ups

Rebrand surface area Sprint 2 deferred and Sprint 3 chose not to expand. Each is a future sprint's anchor.

1. **`goose://` URL scheme** — 14+ literals across `ui/desktop/src/` (sessions, recipes, extensions, scheduling). Per ADR-003 the scheme rewrite must be atomic: `forge.config.ts` `schemes:` + both `.desktop` `MimeType=` + every `src/` consumer in one commit. Also bundles the `OnboardingGuard` "Welcome to goose" string (a `src/` branded literal).
2. **`document.title` runtime overwrite** — `index.html` already sets `<title>Oscar GC</title>`, but React resets the title to "Goose" after first render. Multiple call sites in `src/` (find with `grep -r "document.title" ui/desktop/src/`).
3. **System prompt** at `crates/goose/src/prompts/system.md` — the agent introduces itself as Goose at runtime. First Rust-touch sprint; merits an ADR (the only legitimate `crates/` edit so far).
4. **Icons** under `ui/desktop/src/images/` — `icon.png/.ico/.icns/.svg/-512.png` are all Goose's. Needs Oscar GC visual identity work before replacing.
5. **`ui/desktop/scripts/goosey`** — invokes `goose-app` on PATH (no longer exists under the new brand); plus `ui/desktop/package.json` macOS bundle scripts default `${GOOSE_BUNDLE_NAME:-Goose}`. Defer until the first Linux PATH installer (.deb/.rpm) ships.
